# internship_day5_task
# Contact Form with HTML Validation

This project is a simple contact form built using HTML and CSS.  
It uses HTML5 validation attributes like `required`, `pattern`, and `maxlength` without JavaScript.  
The form includes name, email, phone number, and message fields.  
Proper labels are used to ensure accessibility and clean UI design.  
The project demonstrates basic form validation and responsive styling.